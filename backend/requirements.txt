# Phase 0 — minimal. Grows per phase.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27          # Alpaca REST client + WebSocket test client
websockets>=12.0    # uvicorn[standard] ws support

# dev / test
pytest>=8.0
